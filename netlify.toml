[build]
  command = "ng build"
  publish = "dist/webseite/browser"

[functions]
  [functions.server]
    main_file = "dist/webseite/server/server.mjs"
    node_bundler = "esbuild"  
  [functions."angular-ssr"]
    excluded_patterns = ["^/sitemap\\.xml$"]
